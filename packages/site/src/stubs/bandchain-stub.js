// Stub for @bandprotocol/bandchain.js — not used at runtime by the site.
// The plugin's dist imports it but doesn't use the band module in the
// browser context. This stub prevents Vite/esbuild from crashing during
// dependency optimization.
export const band = {};
export default {};
